# ConfocalOrchestrator dependencies
# Uncomment / pip install packages as each part of the pipeline is actually built.

# --- ND2 file reading (active) ---
nd2==0.11.3
numpy==2.5.1
Pillow==12.3.0

# --- Segmentation (active) ---
cellpose==4.2.1.1

# --- OME-TIFF conversion (active) ---
tifffile==2026.7.31

# --- Acquisition (active) ---
PyYAML==6.0.3
fastapi==0.141.1
uvicorn==0.52.1
mcp==2.0.0

# --- Camera / GenICam (active) --- see acquisition/backends/baumer_genicam.py
harvesters==1.4.3
genicam==1.5.1
opencv-python==5.0.0.93  # Bayer demosaicing only (cv2.cvtColor) - see baumer_genicam.py

# --- Acquisition (future) ---
# aicsimageio

# --- Image Processing & Analysis (active) ---
scikit-image==0.26.0
trackpy==0.7
pandas==3.0.5
matplotlib==3.11.1
pytz==2026.3.post1

# --- System ---
pywin32==312


# --- Dev / Testing ---
# pytest